{#
  Template de connexion utilisateur
  - Hérite du layout principal base.html.twig
  - Affiche le formulaire de connexion et les erreurs éventuelles
#}
{% extends 'base.html.twig' %}

{# Bloc du titre de la page (affiché dans <title>) #}
{% block title %}Connexion{% endblock %}

{# Bloc principal du contenu de la page #}
{% block body %}
  <div class="register-form-wrapper">
    <div class="register-card">
      <div class="register-welcome">
        <h2>🔑 Connectez-vous à TerraBuild</h2>
        <p>Retrouvez votre monde, reprenez vos projets et continuez l’aventure là où vous l’avez laissée.<br>
        Saisissez vos identifiants pour accéder à votre espace et bâtir sans limites.</p>
      </div>
      <h1 class="register-title">Connexion</h1>
      {% if error is defined and error %}
        <div class="login-error">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
      {% endif %}
      <form method="post" action="{{ path('app_login') }}">
        <div class="form-group">
          <label for="inputEmail" class="register-card-label">Adresse email</label>
          <input type="email" class="register-card-input" id="inputEmail" name="_username" value="{{ last_username|default('') }}" required autofocus autocomplete="email">
        </div>
        <div class="form-group">
          <label for="inputPassword" class="register-card-label">Mot de passe</label>
          <input type="password" class="register-card-input" id="inputPassword" name="_password" required autocomplete="current-password">
        </div>
        <button type="submit" class="register-btn">Connexion</button>
      </form>
      <div class="login-forgot">
        <a href="{{ path('app_forgot_password') }}">Mot de passe oublié&nbsp;?</a>
      </div>
    </div>
  </div>
{% endblock %}
